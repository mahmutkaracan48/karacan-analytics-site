# Media folder

Hook overview video was removed (May 2026). Preview pages and the marketing site now lead with **ADA Compliance Risk** + **Estimated Monthly Recoverable Revenue** widgets at the top.

You may keep `accessibility-snapshot-hook.mp4` for archival use; it is no longer referenced by `index.html`, `checkout-config.js`, or `preview_builder.py`.
